<h1>Books Index</h1>
<% @books.each do |book| %>
    <div>
      <h2><%= book.title %></h2>
      <%= link_to 'Show', { action: :show, id: book.id } %>
      <%= link_to 'Edit', { action: :edit, id: book.id } %>
    </div>
<% end %>

<%= link_to 'New', { action: :new }  %>
